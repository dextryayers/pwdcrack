[package]
name = "engine-power"
version = "0.1.0"
edition = "2024"
description = "Power management — RAPL monitoring, frequency scaling, battery-aware"

[dependencies]
log = "0.4"
thiserror = "1"

[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2"
