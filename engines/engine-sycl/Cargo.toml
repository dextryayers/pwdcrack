[package]
name = "engine-sycl"
version = "0.1.0"
edition = "2024"
description = "Intel SYCL compute engine for cross-platform hash cracking (CPU/GPU/FPGA)"

[dependencies]
log = "0.4"
hex = "0.4"
thiserror = "2"
serde = { version = "1", features = ["derive"] }
pwdcrack = { path = "../../" }
