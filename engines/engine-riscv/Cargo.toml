[package]
name = "engine-riscv"
version = "0.1.0"
edition = "2024"
description = "RISC-V Vector extension acceleration for hash cracking"

[dependencies]
md5 = "0.7"

[features]
default = []
riscv = []
